mixed-port: 7890
allow-lan: true
bind-address: '*'
mode: rule
log-level: debug
ipv6: false
external-controller: 127.0.0.1:9090

# TUN 配置（针对 Android 优化）
tun:
  enable: true
  stack: system  # 改回 system，兼容性更好
  auto-route: true
  auto-redirect: true
  dns-hijack:
    - "0.0.0.0:53"
  mtu: 1500
  strict-route: false  # 关闭严格路由，避免问题

# DNS 配置（修复版）
dns:
  enable: true
  listen: 0.0.0.0:1053
  ipv6: false
  enhanced-mode: fake-ip
  fake-ip-range: 198.18.0.1/16
  # 重要：避免这些域名走 fake-ip
  fake-ip-filter:
    - '*.lan'
    - '*.localdomain'
    - '*.localhost'
    - '*.test'
    - '*.local'
    - 'msftconnecttest.com'
    - 'msftncsi.com'
    - 'time.*.com'
    - 'time.*.gov'
    - 'ntp.*.com'
    - 'ntp.*.gov'
    - '+.market.xiaomi.com'
    - '+.vivo.com.cn'
    - '+.vivo.com'
    - '+.qq.com'
    - '+.weixin.qq.com'
    - '+.wechat.com'
    - '+.bing.com'
    - '+.microsoft.com'
    - '+.google.com'
    - '+.googleapis.com'
  default-nameserver:
    - 223.5.5.5
    - 119.29.29.29
  nameserver:
    - 223.5.5.5
    - 119.29.29.29
  fallback:
    - 1.1.1.1
    - 8.8.8.8
  fallback-filter:
    geoip: true
    geoip-code: CN
    ipcidr:
      - 240.0.0.0/4

# 代理节点
proxies:
  - name: "🎯 全球直连"
    type: direct
    udp: true

# 代理组
proxy-groups:
  - name: "🚀 节点选择"
    type: select
    proxies:
      - "🎯 全球直连"

# 规则订阅（去广告）
rule-providers:
  awavenue-ads:
    type: http
    behavior: classical
    format: yaml
    interval: 86400
    url: "https://script.cx.ms/awavenue/AWAvenue-Ads-Rule-Clash-Classical.yaml"
    path: ./ruleset/awavenue-ads.yaml

  adblock-mihomo:
    type: http
    behavior: domain
    format: yaml
    interval: 86400
    url: "https://gcore.jsdelivr.net/gh/217heidai/adblockfilters@main/rules/adblockmihomo.yaml"
    path: ./ruleset/adblock-mihomo.yaml

# 规则
rules:
  # 去广告规则（优先）
  - RULE-SET,awavenue-ads,REJECT
  - RULE-SET,adblock-mihomo,REJECT
  
  # 局域网直连
  - IP-CIDR,127.0.0.0/8,DIRECT,no-resolve
  - IP-CIDR,172.16.0.0/12,DIRECT,no-resolve
  - IP-CIDR,192.168.0.0/16,DIRECT,no-resolve
  - IP-CIDR,10.0.0.0/8,DIRECT,no-resolve
  
  # 其他全部直连
  - MATCH,DIRECT

# 实验性配置
profile:
  store-selected: true
  store-fake-ip: true
